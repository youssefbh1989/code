import 'package:flutter/cupertino.dart';

class Widget_carte extends StatelessWidget {
  const Widget_carte({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
