# salonat

Salonat Project

## Build the app

First run `flutter pub get` to install dependencies <br/><br/>
Then run `flutter pub run build_runner build --delete-conflicting-outputs` to generate hive adapter

### Good luck.